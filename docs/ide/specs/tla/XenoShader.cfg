\* TLC Model Checker configuration for XenoShader.tla

SPECIFICATION
    Spec

\* ────────────────────────────────────────────────────────────────────────────
\* INVARIANTS
\* Predicates that must hold in every reachable state.
\* Add one line per invariant defined in the .tla file.
\* ────────────────────────────────────────────────────────────────────────────

INVARIANT
    TypeOK

\* ────────────────────────────────────────────────────────────────────────────
\* PROPERTIES
\* Temporal formulas (liveness) checked over all execution traces.
\* Uncomment and define these in the .tla file as you add them.
\* ────────────────────────────────────────────────────────────────────────────

\* PROPERTY
\*     EventuallyCompiled
